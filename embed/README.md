# embed

`embeding` 模型测试，运行在 `ollama` 上.

```shell
sudo bash install.sh
```

```shell
python -m venv .venv
```

```shell
source .venv/bin/activate
```

```shell
pip install -r requirements.txt
```

```shell
python test_embed.py
```
